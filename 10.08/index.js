class MyTable {
    constructor(dom, options) {
        this.table = document.createElement("table");
        this.config = {
            style: {
                width: "500px"
            }
        }
    }
}